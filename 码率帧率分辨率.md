# 码率、帧率、分辨率的关系（音视频面试基本公式）

> 锚定真实代码：T23 `joa_video.c` 的码率控制实现（双码流配置、动态调码率、夜间降帧）。
> 这条公式把你已有的三个知识点串成一条线：SD 卡选速（码率→写速）、YUV 帧大小（分辨率→内存）、psrecord 关键帧索引（GOP→索引密度）。

---

## 一、总公式（先背这个）

```
未压缩码率 = 分辨率 × 位深 × 帧率

  例: 1080P YUV420 @ 25fps
      = 1920 × 1080 × 1.5字节 × 8bit × 25
      = 622 Mbps          ← 未压缩，根本存不下/传不动

压缩后码率 = 未压缩码率 ÷ 压缩比

  H.264 压缩比约 100~200 : 1
  1080P @ 25fps H.264 ≈ 4~8 Mbps
  1080P @ 25fps H.265 ≈ 2~4 Mbps   ← H.265 比 H.264 省一半
```

**三者的本质：码率是"总预算"，分辨率和帧率是"花钱的方式"**

```
预算固定（码率不变）时:
  分辨率↑ → 每个像素分到的比特↓ → 画面糊（马赛克）
  帧率↑   → 每一帧分到的比特↓ → 每帧画质↓

想同时高分辨率 + 高帧率 + 高画质 → 码率必须↑
带宽/存储不够 → 在三者之间做取舍
```

---

## 二、各自的感官指标

| 参数 | 影响什么 | 太低的表现 | 经验值 |
|---|---|---|---|
| **分辨率** | 清晰度（细节） | 模糊、放大有马赛克 | 1080P 主流，4K 高端 |
| **帧率** | 流畅度 | < 15fps 人眼明显卡顿 | 监控 25fps（PAL 制式），电影 24fps |
| **码率** | 画质的"预算" | 运动场景糊成块 | 见下表 |

**典型配置表（工程经验值）**：

| 配置 | 码率 | 用途 |
|---|---|---|
| 1024×720 @ 400kbps | T23 实拍主码流（电池设备，低码率） | 电池 IPC |
| 512×288 @ 256kbps | T23 实拍子码流 | 预览/低功耗 |
| 1080P @ 25fps H.264 | 4~8 Mbps | 常规监控 |
| 1080P @ 25fps H.265 | 2~4 Mbps | 新设备 |
| 4K @ 30fps H.265 | 15~25 Mbps | 高端 |

---

## 三、T23 的双码流设计（真实代码）

```
joa_video.c:1877-1886 —— 主/子码流分别配置:

主码流 1024×720:  target_bitrate = 400 kbps   （看录像用）
子码流 512×288:  target_bitrate = 256 kbps   （预览/推送用）

为什么双码流:
  录像要清晰   → 主码流（高分辨率高码率）
  手机预览要流畅 → 子码流（低分辨率低码率，弱网也能传）
  同一个传感器输出，编码器两路并行编码
```

---

## 四、码率控制模式（CBR/VBR）—— T23 实拍

```c
// joa_video.c:1086,1106,1127 —— 三种模式
rcMode = uiBitrateMode;
attrH264Cbr.outBitRate = uiBitrate;   // CBR 恒定码率
attrH264Vbr.maxBitRate = uiBitrate;   // VBR 可变码率
attrH264Smart.maxBitRate = uiBitrate;  // Smart（T23 默认）
```

| 模式 | 码率 | 画质 | 适用 |
|---|---|---|---|
| **CBR**（恒定） | 恒定 | 复杂场景糊 | 直播/推流（带宽固定） |
| **VBR**（可变） | 波动 | 恒定 | 存储/录像（按需分配） |
| **Smart** | 限上限波动 | 智能平衡 | T23 的选择 |

**QP（Quantization Parameter，量化参数）**——码率的实际调节旋钮：

```c
// joa_video.c:1107-1108
attrH264Cbr.maxQp = 45;   // QP 上限：压缩最狠（最糊）
attrH264Cbr.minQp = 15;   // QP 下限：压缩最轻（最清晰）
```

```
码率控制本质 = 动态调 QP:
  画面复杂（运动多）→ 目标码率装不下 → 提高 QP → 压狠一点
  画面简单（静止）  → 预算富余      → 降低 QP → 画质好一点
QP 每增加 6，码率大约减半。
```

---

## 五、GOP 与关键帧（串联 psrecord）

```
GOP（Group of Pictures）= 两个 I 帧之间的帧数

帧类型:
  I 帧（关键帧）: 完整画面，大（几十~几百 KB）★ 可独立解码
  P 帧: 与前一帧的差分，小（几 KB）
  B 帧: 与前后帧的双向差分，更小

// joa_video.c:1090 —— T23 的 GOP 设置
rcAttr.maxGop = outFrmRate.frmRateNum * 6;
// 25fps → GOP = 150 帧 → 每 6 秒一个 I 帧
```

**GOP 的权衡**：

| GOP 大 | GOP 小 |
|---|---|
| I 帧少 → 平均码率低（省存储/带宽） | I 帧多 → 平均码率高 |
| 拖动定位慢（要回退到上一个 I 帧） | 秒开/快进快 |
| 弱网丢包后恢复慢（等下一个 I 帧） | 丢包恢复快 |

**串联你的三个知识点**：

```
① psrecord 关键帧索引:
   索引的密度 = GOP 的密度（每个 I 帧一个索引项）
   → T23 的 PS 文件从末尾 128K 解析 0x000001BD 索引
   → GOP=6秒 → 每 6 秒一个可定位点（回放跳转的粒度）

② SD 卡选速（filesystem.md）:
   码率 4~8Mbps = 0.5~1 MB/s → C10/V10（10MB/s）绰绰有余
   → I 帧瞬时突发大（128KB/帧），但平均码率才是持续写速的约束

③ RTSP/GB28181 推流:
   码率必须 < 网络带宽（留 20% 余量）
   → 8Mbps 码率至少要 10Mbps 带宽
```

---

## 六、T23 的动态码率调整（绝佳实战案例）

```c
// joa_video.c:1889-1954 —— AdjustBitrateByGopSize()
// 思路: 用 GOP 累计大小做反馈，动态调码率

if (GOP累计大小 > 400KB) {          // GOP 太大（编码器输出超预算）
    new_bitrate = bitrate × 85%;      // 降 15%，最低 300kbps
    VE_set_video_bitrate(chn, new_bitrate);  // ★ 在线改码率（不停流）
}
else if (GOP累计大小 < 目标×0.7) {    // GOP 太小（预算富余）
    new_bitrate = bitrate × 115%;     // 升 15%，最高 400kbps
    VE_set_video_bitrate(chn, new_bitrate);
}
```

```
这是"码率-画质-存储"闭环控制的活教材:
  监控 GOP 实际大小 → 反馈 → 在线调码率 → 把 GOP 压回目标区间
  → 存储占用可预测（SD 卡循环录像的写速稳定）
```

## 七、夜间降帧（帧率权衡的真实案例）

```c
// joa_video.c:285
//nfps = 8; // in night mode, will decrease framerate to 8
```

```
为什么夜间降帧:
  低照度 → 传感器单位时间收到的光少 → 噪声大
  对策: 降低帧率 → 单帧曝光时间变长（25fps 每帧 40ms，
        8fps 每帧 125ms）→ 信噪比提升 → 画面更干净

同时码率也降:
  帧率 25→8 → 每秒数据量降到 1/3 → 弱网/电池场景友好
  代价: 流畅度下降（监控场景夜间可接受）
```

**这是"帧率是预算分配方式"的最佳注脚**：夜间用帧率换画质（单帧曝光）+ 省电。

---

## 八、面试问答

**Q：码率、帧率、分辨率什么关系？**

> 未压缩码率 = 分辨率 × 位深 × 帧率，1080P YUV420@25fps 约 622Mbps；H.264 压缩比 100~200 倍后约 4~8Mbps。码率是总预算：分辨率和帧率是花钱方式——码率固定时分辨率↑则画质↓、帧率↑则每帧画质↓。所以低带宽要么降分辨率（子码流），要么降帧率（夜间 8fps），要么换更狠的编码（H.265 省 H.264 一半）。

**Q：CBR 和 VBR 怎么选？**

> CBR 恒定码率，复杂场景画质下降——适合带宽固定的推流/直播；VBR 码率波动画质稳定——适合存储录像。T23 用 Smart 模式（限上限的 VBR），还做了闭环：监测 GOP 累计大小，超了在线降码率 15%，富余了升 15%（`VE_set_video_bitrate` 不停流热调），保证 SD 卡循环录像写速可预测。

**Q：GOP 怎么设？和录像回放什么关系？**

> GOP = 两个 I 帧间隔。T23 设 6 秒（25fps×6=150 帧）。GOP 大省码率但拖动定位慢——回放跳转必须落在 I 帧上，psrecord 的关键帧索引就是按 I 帧记录的，GOP 决定了回放跳转粒度和索引密度。弱网丢包恢复也要等下一个 I 帧，所以直播类倾向小 GOP，存储类可以大。

**Q：为什么夜间要降帧？**

> 低照度传感器噪声大。降帧（25→8fps）让单帧曝光时间从 40ms 增到 125ms，信噪比提升画质更干净，同时码率降到 1/3。这是"用帧率换画质 + 省电"的典型工程权衡。

---

## 九、收尾标准

- [ ] 能张口算：1080P YUV420 未压缩码率 ≈ 622Mbps、H.264 压缩后 4~8Mbps
- [ ] 能讲"码率是预算"的分配逻辑（分辨率/帧率/画质三角）
- [ ] 能讲 T23 的双码流（1024×720@400k + 512×288@256k）为什么这么设计
- [ ] 能讲 GOP 与 psrecord 索引、弱网恢复的关系
- [ ] 能讲夜间降帧的原理（曝光时间换信噪比）
